name: Post Deploy CI

on:
  workflow_dispatch:
  push:
    branches:
      - 'feature/*'
    paths:
      - 'deploy/environments/dev/values.yaml'
    tags-ignore:
      - '**'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  call-ci:
    name: Post Deploy CI
    uses: mdefenders/shared-workflows/.github/workflows/post-deploy-ci.yaml@dev
    secrets: inherit